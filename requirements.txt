# Web
Flask==3.0.3
gunicorn==21.2.0
requests==2.32.3
beautifulsoup4==4.12.3
lxml>=5.3,<6
line-bot-sdk>=3.11,<4
flask-cors==4.0.1

# Google Cloud
google-cloud-firestore==2.16.0
# google-cloud-tasks==2.16.3   # 目前沒用到可先註解
grpcio>=1.74,<1.76

# 瀏覽器自動化（Selenium 優先）
selenium==4.26.1
# webdriver-manager 會在執行期下載，Cloud Run 不建議 → 移除

# Playwright 僅在 Linux/Cloud Run 裝；Windows 本機跳過
playwright>=1.47,<1.48 ; sys_platform != "win32"